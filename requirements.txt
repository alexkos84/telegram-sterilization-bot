# Основные зависимости
python-dotenv==1.0.0      # Для загрузки переменных окружения
loguru==0.7.2             # Удобное логирование (альтернатива стандартному logging)

# Telegram бот
pyTelegramBotAPI==4.14.0   # Основная библиотека бота
Flask==3.0.0              # Веб-сервер для webhook
gunicorn==21.2.0          # Для production-развертывания Flask (опционально)

# Парсинг и работа с данными
requests==2.31.0          # HTTP-запросы
beautifulsoup4==4.12.2    # Парсинг HTML
lxml==5.1.0               # Быстрый парсер HTML (лучше html.parser для сложных случаев)
cloudscraper==1.2.71      # Обход Cloudflare
fake-useragent==1.3.0     # Ротация User-Agent (полезно для парсинга)

# Работа с медиа
Pillow==10.1.0            # Обработка изображений
python-magic==0.4.27      # Определение типов файлов
aiohttp==3.9.1            # Асинхронные запросы для медиа
aiogram==2.25.1           # Асинхронная альтернатива pyTelegramBotAPI (опционально)

# Кэширование и производительность
redis==5.0.1              # Для кэширования результатов парсинга (опционально)
pymongo==4.6.0            # Если нужно хранить данные в MongoDB (опционально)

# Дополнительные утилиты
dateparser==1.2.0         # Парсинг дат из текста (для обработки дат в постах)
pytz==2023.3              # Работа с часовыми поясами
tqdm==4.66.1              # Индикаторы прогресса для долгих операций


